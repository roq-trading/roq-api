dist: trusty
language: cpp
compiler: gcc
sudo: required
script:
 - gcc --version
 - ./autogen.sh && ./configure && make -j2 && make -j2 check && make lint
before_install: scripts/ubuntu.sh
install:
 - scripts/install-glog.sh
 - scripts/install-gflags.sh
 - scripts/install-double-conversion.sh
 - scripts/install-cctz.sh
 - scripts/install-flatbuffers.sh
 - scripts/install-libevent.sh
