include "update_action.fbs";

namespace roq.fbs;

table MBOUpdate {
  price:double = nan;
  remaining_quantity:double = 0.0;
  priority:uint64 = 0;
  order_id:string;
  action:UpdateAction;
  reason:UpdateReason;
}
