include "connection_status.fbs";
include "priority.fbs";
include "stream_type.fbs";

namespace roq.fbs;

table StreamStatus {
  stream_id:uint16;
  type:StreamType;
  supports:uint64;
  account:string;
  priority:Priority;
  status:ConnectionStatus;
}
