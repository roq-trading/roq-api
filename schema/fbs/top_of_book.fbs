include "layer.fbs";

namespace roq.fbs;

table TopOfBook {
  stream_id:uint16;
  exchange:string;
  symbol:string;
  layer:Layer;
  update_type:UpdateType;
  exchange_time_utc:int64;
  exchange_sequence:int64;
  sending_time_utc:int64;
}
